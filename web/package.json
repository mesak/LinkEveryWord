{
  "name": "linkeveryword-web",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "wrangler pages dev .",
    "preview": "wrangler pages dev .",
    "deploy": "wrangler pages deploy .",
    "typecheck": "tsc --noEmit",
    "sync:privacy": "node scripts/sync-privacy.mjs",
    "postinstall": "npm run sync:privacy"
  },
  "dependencies": {
    "hono": "^4.5.7",
    "marked": "^12.0.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240821.0",
    "typescript": "^5.5.4",
    "wrangler": "^4.33.1"
  }
}
