// Fallback privacy content to satisfy bundlers if language-specific files are present.
// The app prefers `privacy.zh.ts` / `privacy.en.ts`; this module is a safe default.
const text = ''
export default text
